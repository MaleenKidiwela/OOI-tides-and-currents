# Core scientific stack used by the notebooks and oceanlib.py
obspy>=1.4
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7

# Jupyter (to run / regenerate the notebooks)
jupyterlab>=4.0
nbformat>=5.9
nbconvert>=7.0

# Web viewer (webapp/)
flask>=3.0

# Optional but recommended: enables the latitude-aware tidal model in the web
# viewer's "de-tide" view and cross-checks notebook 04. Without it, oceanlib
# falls back to the built-in NumPy least-squares fit.
# utide>=0.3
